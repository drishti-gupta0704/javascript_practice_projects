
## 🧾 Simple Registration Form

its a basic registration form validation using JavaScript.
It checks whether all fields are filled, the email format is valid, and the password meets the required length.

### [click to view live project](https://inspiring-travesseiro-4356cd.netlify.app/)

### Features
- Checks if all fields are filled
- Validates email format using Regular Expressions (Regex)
- Ensures password is at least 6 characters long
- Displays inline error messages dynamically
- Resets form automatically after successful registration

🛠️ **Tech Stack**

HTML
CSS
JavaScript 